
services:
  postgres:
    container_name: todoForYou
    image: postgres
    ports:
    - 5432:5432
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: tododb
      PGDATA: /data/postgres
    volumes:
    - ./data/pg:/data/postgres